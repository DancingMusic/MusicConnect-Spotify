{
  "name": "@dancingmusic/music-connect-spotify",
  "version": "0.1.0",
  "description": "Spotify connector for DancingMusic. Uses the official Spotify Web API (search + 30s preview URLs). Requires an access token.",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "exports": {
    ".": { "import": "./dist/index.js" }
  },
  "files": ["dist", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsup src/index.ts --format esm --clean --target es2020 --no-splitting",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["dancingmusic", "music-connector", "spotify"],
  "license": "MIT",
  "peerDependencies": {
    "@dancingmusic/music-store": ">=0.1.0"
  },
  "devDependencies": {
    "@dancingmusic/music-store": "file:/Users/wuguangliang/workspace/DancingMusicWorkspace/DancingMusic/.claude/worktrees/elastic-bhabha-ada1fe/submodules/MusicStore",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  }
}
